apiVersion: v1
kind: Service
metadata:
  name: heart-disease-api-service
  namespace: default
  labels:
    app: heart-disease-api
spec:
  selector:
    app: heart-disease-api
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
